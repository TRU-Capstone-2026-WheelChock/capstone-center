services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        USERNAME: dev
        UID: 1000
        GID: 1000
    volumes:
      - .:/app:cached
    command: sleep infinity
