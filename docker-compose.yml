services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        USERNAME: dev
        UID: 1000
        GID: 1000
    working_dir: /app
    volumes:
      - .:/app:cached
    stdin_open: true
    tty: true
    init: true
    command: sleep infinity
