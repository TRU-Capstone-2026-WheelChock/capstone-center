services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        USERNAME: dev
        UID: 1000
        GID: 1000
    volumes:
      - .:/app:cached
      - poetry-venv:/app/.venv
    env_file:
      - .env_dev
    command: sleep infinity

volumes:
  poetry-venv: